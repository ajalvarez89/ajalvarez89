# ============================================================================
# crypto-trading-bot — Environment configuration
# ============================================================================
# Copy this file to .env and fill in the values. NEVER commit .env.
# Set restrictive permissions: chmod 600 .env
# ============================================================================

# ----------------------------------------------------------------------------
# Trading mode: testnet | paper | live
# - testnet: connects to Binance Testnet (https://testnet.binance.vision)
# - paper:   uses paper_broker locally (no real exchange calls)
# - live:    REAL MONEY. Requires I_UNDERSTAND_RISK=yes and prod compose override.
# ----------------------------------------------------------------------------
TRADING_MODE=testnet
I_UNDERSTAND_RISK=no

# ----------------------------------------------------------------------------
# Binance API (use Testnet keys by default)
# Permissions required: Reading + Spot Trading. NEVER enable Withdrawals.
# Activate IP whitelist in Binance UI.
# ----------------------------------------------------------------------------
BINANCE_API_KEY=replace_me
BINANCE_API_SECRET=replace_me
BINANCE_TESTNET=1

# Capital cap when TRADING_MODE=live (USDT)
LIVE_CAPITAL_CAP_USDT=200

# ----------------------------------------------------------------------------
# Pairs to track
# ----------------------------------------------------------------------------
TRADING_PAIRS=BTCUSDT,ETHUSDT,SOLUSDT
DEFAULT_TIMEFRAME=5m

# ----------------------------------------------------------------------------
# Phoenix
# ----------------------------------------------------------------------------
PHX_HOST=localhost
PHX_PORT=4000
SECRET_KEY_BASE=replace_with_64_chars_min_use_mix_phx_gen_secret
DATABASE_URL=sqlite:///data/sqlite/trading.db

# ----------------------------------------------------------------------------
# Redis
# ----------------------------------------------------------------------------
REDIS_URL=redis://redis:6379/0

# ----------------------------------------------------------------------------
# Trading engine
# ----------------------------------------------------------------------------
TRADING_ENGINE_PORT=8001
TRADING_ENGINE_HOST=trading_engine

# ----------------------------------------------------------------------------
# ML service
# ----------------------------------------------------------------------------
ML_SERVICE_PORT=8002
ML_SERVICE_HOST=ml_service
DUCKDB_PATH=/data/duckdb/warehouse.duckdb

# ----------------------------------------------------------------------------
# News collector
# ----------------------------------------------------------------------------
NEWS_COLLECTOR_PORT=8003
CRYPTOPANIC_API_KEY=
NEWSAPI_KEY=
NEWS_POLL_INTERVAL_SEC=60

# ----------------------------------------------------------------------------
# Web (Next.js)
# ----------------------------------------------------------------------------
WEB_PORT=3000
NEXT_PUBLIC_PHOENIX_WS_URL=ws://localhost:4000/socket
NEXT_PUBLIC_PHOENIX_API_URL=http://localhost:4000/api

# ----------------------------------------------------------------------------
# Logging
# ----------------------------------------------------------------------------
LOG_LEVEL=info
LOG_FORMAT=json
