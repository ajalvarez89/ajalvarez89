bind 0.0.0.0
protected-mode no
port 6379
appendonly yes
appendfsync everysec
maxmemory 256mb
maxmemory-policy allkeys-lru

# Streams retention: keep last 100k entries per stream automatically via XADD MAXLEN ~
# (enforced by producers, not by config)
